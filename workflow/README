WORKFLOW XML TEMPLATES AND INI FILES


BEFORE RUNNING make sure you run this command:
assuming you already defined $PERL5LIB
setenv PERL5LIB ${PERL5LIB}:/usr/local/annotation/PNEUMO/clu_dir/papyrus/src/perl


*PEffect workflow runs AFTER AllvsAll workflow
*bit_score workflow is independent of the other 2
*For help:  http://intranet/ifx/devel/workflow/main_frame.html



AllvsAll workflow:
config file:  all_vs_all.ini
template:     all_vs_all_template.xml

A different config file is required for each assembly.
Just do a global search and replace to generate a new workflow config file
replacing old assembly with a new one.

To Execute: (sample)

RunWorkflow -t all_vs_all_template.xml -c all_vs_all.ini -i allvsall.xml -l allvsall.log

replace -i and -l arugment with appropriate file name
------------------------------------------------------------------------------------------------

PEffect workflow:
config file:  pe.ini
template:     pe_template.xml

A different config file is required for each assembly.
Just do a global search and replace to generate a new workflow config file
replacing old assembly with a new one.

To Execute: (sample)

RunWorkflow -t pe_template.xml -c pe.ini -i pe.xml -l pe.log

replace -i and -l arugment with appropriate file name

------------------------------------------------------------------------------------------------

bit_score workflow
config file:  bit_score.ini
template:     bit_score_template.xml

A different config file is required for each assembly.
Just do a global search and replace to generate a new workflow config file
replacing old assembly with a new one.

To Execute: (sample)

RunWorkflow -t bit_score_template.xml -c bit_score.ini -i bitscore.xml -l bitscore.log

replace -i and -l arugment with appropriate file name

#set environment
setenv PERL5LIB ${PERL5LIB}:/usr/local/annotation/PNEUMO/clu_dir/papyrus/src/perl
setenv WF_ROOT /usr/local/devel/ANNOTATION/workflow
setenv PATH ${PATH}:/usr/local/devel/ANNOTATION/workflow

ntsp01_1_assembly IN PROGRESS                                                           
ntsp02_2_assembly                                                               
gbs18rs21_797_assembly                                                          
gbs515_318_assembly                                                
gbscjb111_200_assembly                                                          
gbscoh1_483_assembly                                                            
gbsa909_747_assembly                                                            
gbsh36b_475_assembly                                                            
gpa_1200_assembly                                                               
ntsa04_2_assembly                                                               
gmi_953_assembly                                                                

bsp_3839_assembly DONE 
gbs_799_assembly DONE
